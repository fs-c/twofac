import styled from 'styled-components';

const Card = styled.div`
    padding: 1em;
    text-align: center;
    border-radius: 5px;
    color: var(--accents-6);
    border: 1px solid var(--accents-6);
`;

export default Card;
